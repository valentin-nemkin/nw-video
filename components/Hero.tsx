'use client';

export default function Hero() {
  return (
    <section className="flex flex-col items-center justify-center h-[100vh] bg-primary text-foreground">
      <h1 className="text-4xl font-secondary">Hero Section</h1>
      <p className="mt-4 text-lg font-primary">Это тестовая заглушка Hero</p>
    </section>
  );
}
